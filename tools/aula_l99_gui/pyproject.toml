[project]
name = "aula-l99-gui"
version = "0.1.0"
description = "PySide6 GUI for the AULA L99 keyboard + touchscreen tools"
requires-python = ">=3.10"
dependencies = [
    "PySide6>=6.5",
    "Pillow>=10.0",
]

[tool.uv]
package = false
